# NorthwindDB-SQL-Schema
MySQL Challenge for Netflix

---

## Project Goals: 

- Use SELECT statements in SQL queries.
- Create specific queries using the WHERE clause and LIKE operator.
- Sort query results using the ORDER BY keyword.
